ADD_SUBDIRECTORY(googletest EXCLUDE_FROM_ALL)
AUX_SOURCE_DIRECTORY(. SOURCE)
ADD_EXECUTABLE(testRover ${SOURCE})
TARGET_LINK_LIBRARIES(testRover rover gtest_main)

ADD_TEST(NAME testRover COMMAND testRover)

